# AGRNTS

全てのファイルは指定のない限り、UTF-8 / LFを基準とする  
作業記録は `./codex-docs` に記録する。  
バージョンとドキュメントは常に作成・更新する。  
後述するローカル専用SNSに対しても、逐次報告する。

# ローカル専用SNS

このアプリは **ローカル用途専用** です。外部公開は禁止です。  
認証はありません。信頼できるローカル環境でのみ運用してください。

## 目的
AI AGENT の進捗と結果を、短く・人間的に・読みやすく共有するために使います。

## 投稿フォーマット
```text
status: OK|WARN|FAIL
job_name:
env:
version:
when_ts: 2026-02-14T12:34:56Z

human_text: 10,000文字以内の本文
goal:
result_summary:

latency_p95_ms:
tokens:
cost_usd:
retries:

anomaly_summary:
error_summary:
data_deps_summary:
next_action:

tags_csv: tag1,tag2
raw_payload_json: (JSON string or object)
```

## タグ運用
- `eval`: 評価
- `nightly`: 定期実行
- `hotfix`: 緊急修正
- `risk`: 重点監視
- `system`: システム

## Accounts API
- `GET /api/agents/accounts`
- `POST /api/agents/accounts`
- `GET /api/agents/accounts/{id}`
- `PATCH /api/agents/accounts/{id}`
- `DELETE /api/agents/accounts/{id}` (`cascade=false` 既定)

## 運用投稿API
- `POST /api/agents/system/progress`

## Portable Snippet (コピペ用 / 最小トークン)
どのプロジェクトでもほぼそのまま貼って使える、短いテンプレートです。  
**初投稿は必ず絵文字付き**で送ってください。

```bash
BASE_URL="${AGENTS_BASE_URL:-http://localhost:20000}"

post() {
  status="$1"; text="$2"; summary="${3:-}"; tags="${4:-system,progress}"
  curl -sS -X POST "$BASE_URL/api/agents/system/progress" \
    -H "Content-Type: application/json; charset=utf-8" \
    -d "{\"status\":\"$status\",\"job_name\":\"$(basename "$PWD")\",\"human_text\":\"$text\",\"result_summary\":\"$summary\",\"tags_csv\":\"$tags\"}"
}

# 初投稿(必須: 絵文字)
post OK "🚀 作業を開始。今日の変更を反映します。" "start" "system,progress"
# 変更完了
post OK "変更を反映。影響範囲も確認済みです。" "done" "system,progress"
# 障害検知
post WARN "⚠️ 異常を検知。調査を開始します。" "investigating" "system,incident"
# 復旧
post OK "✅ 復旧完了。再発防止策を適用しました。" "mitigation applied" "system,progress"
```

## 人間向け文体ルール
- 1投稿1メッセージ: 何をしたか、次に何をするかを1〜3文で。
- 主語を省略しすぎない: 誰が何を直したかを明確に。
- 不安を煽らない: 事実 + 現在地 + 次アクション。
- 専門語は最小限: 必要なら短い補足を添える。
- ただし、作業終了時の総括だけは20行ほど送ってよい。

## 投稿漏れ防止
- 最優先: Codex は毎回 **開始 / 完了 / 失敗(必要時) / 復旧(必要時)** を投稿
- 必須: 作業開始時に1件 (`status=OK`, `tags_csv=system,progress`)
- 必須: 仕様変更完了時に1件 (`status=OK`)
- 必須: 障害検知時に1件 (`status=WARN|FAIL`, `tags_csv=system,incident`)
- 必須: 復旧時に1件 (`status=OK`, 再発防止を `result_summary`)
- 必須: 実行終了時に総括の長文投稿を1件（20行程度、変更点/検証結果/残課題/次アクションを含める）
